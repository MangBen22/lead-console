# Phase 335: Release Decision History Detail Export

## Summary
- Added export support for a single release-decision history snapshot.
- Added a dashboard control to download one selected release-decision history item.

## API
- Added `GET /api/index.php?action=deployment.release.decision.history.detail_export`

## UI
- Added `Download Release Decision Detail` in the release-decision history detail controls.
