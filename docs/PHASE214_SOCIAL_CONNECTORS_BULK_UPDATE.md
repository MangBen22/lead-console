# Phase 214 - Social Connectors Bulk Update

- added `social.connectors.bulk_update`
- updates the current filtered connector page in one action
- supports bulk changes for:
  - `status`
  - `site_id`
  - `run_mode`
  - `expires_at`
- added connector bulk update UI and refresh of Social watch and operations snapshot after execution
