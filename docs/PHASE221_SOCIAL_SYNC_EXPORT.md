# Phase 221 - Social Sync Export

- added `social.push.export`
- exports the filtered sync log
- can optionally include the currently selected sync detail payload
- added sync export action in the Social push pipeline workspace
