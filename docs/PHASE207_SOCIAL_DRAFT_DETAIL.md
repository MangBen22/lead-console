# Phase 207 - Social Draft Detail

- added `social.drafts.detail`
- supports loading a generated draft by:
  - `draft_index`
  - `lead_id`
- returns per-draft metadata including:
  - URL availability
  - message length
  - ready connector count
  - blocked connector count
- added draft detail UI and schedule form prefill from the selected draft
