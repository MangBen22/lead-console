# Phase 217 - Social Retry Export

- added `social.retry.export`
- exports the filtered retry queue
- can optionally include the currently selected retry detail payload
- added retry export action in the Social push pipeline workspace
