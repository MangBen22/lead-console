# Phase 254: SEO URL History Export

## Summary
- Added export support for the SEO URL history view.

## API
- `seo.url.history.export`
  - Accepts optional `project_id`
  - Accepts optional `url`

## UI
- Added `Download URL History`

## Notes
- Export includes both aggregated URL rows and the timeline slice used by the live view.
