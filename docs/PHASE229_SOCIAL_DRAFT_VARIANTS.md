# Phase 229 - Social Draft Variants

- added `social.drafts.variants`
- builds connector-specific draft variants for a selected draft
- variants now include provider-specific shaping such as:
  - generated titles
  - appended URLs
  - appended hashtags
  - trimmed X payloads
- added draft variants UI and automatic refresh after draft detail loads
