# Phase 231 - Social Provider Rules

- added `social_provider_rule_profile()` for provider-specific transport and content rules
- Social platform detail now exposes:
  - provider rules
  - connector summary
  - live vs dry-run counts
  - active connector counts
- platform detail is now aligned with the runtime variant and validation behavior
