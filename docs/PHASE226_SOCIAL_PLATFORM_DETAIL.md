# Phase 226 - Social Platform Detail

- added `social.platforms.detail`
- loads one provider profile by `provider`
- returns:
  - provider profile
  - connector usage count
  - auth mode count
  - capability count
- added platform detail UI and provider form prefill
