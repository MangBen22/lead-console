# Phase 236 - Social Connector Rule Audit Detail

- added `social.connectors.rule_audit_detail`
- loads a single connector audit view by `connector_id`
- returns:
  - provider rules
  - readiness
  - validation
  - connector detail
  - sample variant bundles
- added connector rule audit detail UI
