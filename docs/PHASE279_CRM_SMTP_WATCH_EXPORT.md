# Phase 279: CRM SMTP Watch Export

## Summary
- Added export coverage for the CRM SMTP watch state and recent runs.

## API
- `crm.smtp.watch.export`
  - Returns:
    - current SMTP watch state
    - latest run
    - recent SMTP watch runs

## UI
- Added a CRM SMTP watch export action in SMTP operations.
