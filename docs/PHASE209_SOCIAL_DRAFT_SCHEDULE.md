# Phase 209 - Social Draft Schedule

- added `social.drafts.schedule`
- allows one social draft to be converted into one queued schedule item
- supports lookup by:
  - `draft_index`
  - `lead_id`
- automatically falls back to the top recommended ready connectors when none are provided
- added draft schedule action UI and refresh of schedule and activity views after creation
