# Phase 274: CRM Email Template Sites

## Summary
- Added a cross-site CRM email template inventory.

## API
- `crm.email_templates.sites`
  - Supports:
    - `site_id`
    - `bridge`
    - `template_key`
    - `search`
    - `page`
    - `limit`

## UI
- Added CRM email template site filters and inventory output.
- Template refresh now reloads both the site inventory and the selected site template detail.
