# Phase 213 - Social Connectors Export

- added `social.connectors.export`
- exports the filtered connector inventory
- can optionally include the currently selected connector detail payload
- added connector export action in the Social connectors workspace
